{
  "name": "dailytrial-md-preview",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "vite build --watch",
    "build": "vite build && npm run icons",
    "icons": "for size in 16 48 128; do qlmanage -t -s $size -o dist/icons dist/icons/icon-$size.svg 2>/dev/null && mv dist/icons/icon-$size.svg.png dist/icons/icon-$size.png; done"
  },
  "devDependencies": {
    "@types/markdown-it": "^14.1.2",
    "typescript": "^5.3.3",
    "vite": "^5.4.0"
  },
  "dependencies": {
    "highlight.js": "^11.10.0",
    "markdown-it": "^14.1.0"
  }
}
